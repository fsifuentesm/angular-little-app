# App Works in Angular
A little app with Angular, the purpose its learn.

## Install:

#### You need install first node.js

`sudo apt install npm`

`sudo apt install ng-common`

`sudo npm install -g angular-cli`

`ng serve`
